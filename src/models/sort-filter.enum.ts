export enum SortFilterEnum {
  DATE_DESC = "dateDesc", // latest first
  DATE_ASC = "dateAsc",   // oldest first
  PRICE_LOW = "priceLow", // lowest price first
  PRICE_HIGH = "priceHigh" // highest price first
}